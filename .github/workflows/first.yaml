name: Shell Commands

on: [push]

jobs:
  run-shell-command:
      runs-on: ubuntu-latest
      steps:
        - name: Print a string
          run: echo "Hello world"
        - name: Multiline script
          run: |
            node -v
            npm -v
